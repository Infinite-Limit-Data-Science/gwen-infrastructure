terraform {
  backend "s3" {
    bucket       = "replace-with-state-bucket"
    key          = "gwen/dev/memory.tfstate"
    region       = "us-east-1"
    encrypt      = true
    use_lockfile = true
  }
}
