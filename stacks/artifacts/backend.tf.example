terraform {
  backend "s3" {
    bucket       = "replace-with-state-bucket"
    key          = "gwen/dev/artifacts.tfstate"
    region       = "us-east-1"
    encrypt      = true
    use_lockfile = true
  }
}
