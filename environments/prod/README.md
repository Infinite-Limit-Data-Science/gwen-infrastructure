# Production Environment

Production uses the same roots with production-specific state keys, Entra
tenant identities, retention, encryption, and change controls. Never reuse a
development or stage state file.
